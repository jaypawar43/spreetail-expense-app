# Render process types
# "release" runs once before each deploy — applies DB migrations automatically
# "web" is the long-running HTTP server

release: python manage.py migrate --noinput && python manage.py collectstatic --noinput --clear
web: gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --workers 3 --timeout 120
